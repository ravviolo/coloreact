import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  PalettePage: {
    height: "100vh",
    display: "flex",
    flexDirection: "column",
    overflow: "hidden",
  },
});
